# Example environment file (do NOT commit real secrets)
# Copy to .env and fill with real keys for local development.

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini

# Azure (if used)
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT=gpt-4o

# PE site credentials (optional for booking skill)
PE_EMAIL=
PE_PASSWORD=
